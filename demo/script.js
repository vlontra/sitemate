const menuToggle=document.querySelector('.menu-toggle');const siteNav=document.querySelector('.site-nav');menuToggle?.addEventListener('click',()=>{const open=siteNav.classList.toggle('open');menuToggle.setAttribute('aria-expanded',String(open));menuToggle.setAttribute('aria-label',open?'Close menu':'Open menu')});siteNav?.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>{siteNav.classList.remove('open');menuToggle.setAttribute('aria-expanded','false');menuToggle.setAttribute('aria-label','Open menu')}));
const comparison=document.querySelector('[data-comparison]');comparison?.querySelector('input')?.addEventListener('input',event=>comparison.style.setProperty('--split',`${event.target.value}%`));
const form=document.querySelector('#estimate-form');form?.addEventListener('submit',event=>{event.preventDefault();if(!form.reportValidity())return;const feedback=form.querySelector('.form-feedback');feedback.hidden=false;feedback.textContent='Demo complete: on a live site, your enquiry would be delivered to the business. Nothing was sent or stored.'});document.querySelector('#year').textContent=new Date().getFullYear();
const launcher=document.querySelector('.chat-launcher');const panel=document.querySelector('.chat-panel');const teaser=document.querySelector('.chat-teaser');const closeChat=document.querySelector('.chat-close');const messages=document.querySelector('.chat-messages');const chatForm=document.querySelector('.chat-form');const chatInput=document.querySelector('#chat-input');function setChat(open){panel.hidden=!open;launcher.setAttribute('aria-expanded',String(open));launcher.setAttribute('aria-label',open?'Close demo chat':'Open demo chat');if(open){teaser.hidden=true;chatInput.focus()}}launcher.addEventListener('click',()=>setChat(panel.hidden));closeChat.addEventListener('click',()=>setChat(false));document.querySelector('.teaser-close').addEventListener('click',()=>teaser.hidden=true);
function answer(question){const q=question.toLowerCase();if(/service|design|garden|lawn|patio|paving|plant|landscap/.test(q))return 'This demo company offers landscape design, garden installation, lawn care, stonework, planting and outdoor living spaces. You can explore each service further up the page.';if(/where|area|location|cover|work|london|surrey/.test(q))return 'In this fictional example, the team serves Greater London and Surrey. A real site would list its verified service areas here.';if(/quote|estimate|price|cost|book|contact/.test(q))return 'Use the “Get a free estimate” button to see the sample enquiry form. This demo does not send or store messages.';if(/human|person|real|ai/.test(q))return 'I am a scripted demonstration of a chat widget, not a live AI agent or a person. A production version could be connected to a real assistant and lead inbox.';return 'I can help with services, service areas and how an estimate request would work. This is a scripted demo, so no message is sent to a business.'}
function addMessage(text,who){const el=document.createElement('div');el.className=`chat-message ${who}`;el.textContent=text;messages.appendChild(el);messages.scrollTop=messages.scrollHeight}function sendQuestion(question){const text=question.trim();if(!text)return;addMessage(text,'user');window.setTimeout(()=>addMessage(answer(text),'bot'),250)}chatForm.addEventListener('submit',event=>{event.preventDefault();sendQuestion(chatInput.value);chatInput.value=''});document.querySelectorAll('[data-chat-question]').forEach(button=>button.addEventListener('click',()=>sendQuestion(button.dataset.chatQuestion)));
